<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       x:Class="SoFit.AppShell">
    <TabBar>
        <ShellContent Title="Home" ContentTemplate="{DataTemplate local:HomePage}" />
        <ShellContent Title="Split" ContentTemplate="{DataTemplate local:SplitPage}" />
        <ShellContent Title="Leaderboard" ContentTemplate="{DataTemplate local:LeaderboardPage}" />
        <ShellContent Title="Calendar" ContentTemplate="{DataTemplate local:CalendarPage}" />
        <ShellContent Title="Settings" ContentTemplate="{DataTemplate local:SettingsPage}" />
    </TabBar>
</Shell>